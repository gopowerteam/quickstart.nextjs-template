export class {{className}} extends Model {
{{#each properties}}
  {{#if description}}
  /**
   * {{description}}
   */
  {{/if}}
  {{#if originalRef}}
  {{#if (unequal originalRef propertyType)}}
  @Type(() => {{originalRef}})
  {{/if}}
  {{/if}}
  public {{propertyName}} : {{propertyType}}
{{/each}}
}
